# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    admin.user:
        class: App\Admin\UserAdmin
        tags:
            - { name: sonata.admin, model_class: App\Entity\User, controller: ~, manager_type: orm, group: admin, label: User }

    admin.cliente:
        class: App\Admin\ClienteAdmin
        tags:
            - { name: sonata.admin, model_class: App\Entity\Cliente, controller: ~, manager_type: orm, group: admin, label: Cliente }

    admin.proveedor:
        class: App\Admin\ProveedorAdmin
        tags:
            - { name: sonata.admin, model_class: App\Entity\Proveedor, controller: ~, manager_type: orm, group: admin, label: Proveedor }

    admin.producto:
        class: App\Admin\ProductoAdmin
        tags:
            - { name: sonata.admin, model_class: App\Entity\Producto, controller: ~, manager_type: orm, group: admin, label: Producto }

    admin.venta:
        class: App\Admin\VentaAdmin
        tags:
            - { name: sonata.admin, model_class: App\Entity\Venta, controller: ~, manager_type: orm, group: admin, label: Venta }

    admin.detalle_venta:
        class: App\Admin\DetalleVentaAdmin
        tags:
            - { name: sonata.admin, model_class: App\Entity\DetalleVenta, controller: ~, manager_type: orm, group: admin, label: DetalleVenta }
